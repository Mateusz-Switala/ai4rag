[build-system]
requires = ["setuptools", "wheel", "build"]
build-backend = "setuptools.build_meta"

[project]
name = "ai4rag"
description = "Automatic and optimized RAG Pattern generator"
authors = [{ name = "IBM" }]
maintainers = [
  { name = "Jakub Walaszczyk", email = "jwalaszc@redhat.com" },
  { name = "Lukasz Cmielowski", email = "lcmielow@redhat.com" },
  { name = "Michal Steczko", email = "msteczko@redhat.com" },
  { name = "Filip Komarzyniec", email = "fkomarzy@redhat.com" },
]
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.12,<3.14"
classifiers = [
  "Development Status :: 4 - Beta",
  "License :: OSI Approved :: Apache Software License",
  "Natural Language :: English",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Operating System :: MacOS :: MacOS X",
  "Operating System :: POSIX :: Linux",
]
keywords = ["AI", "RAG", "LLM", "GenAI"]
dynamic = ["version"]

dependencies = [
  "boto3>=1.28",
  "docling-slim[feat-chunking]~=2.121.0",
  "json-repair>=0.30,<1",
  "neo4j>=5.0,<6",
  "neo4j-graphrag>=1.0,<2",
  "langchain-text-splitters~=1.1.0",
  "multiprocess>=0.70",
  "openai~=2.53.0",
  "pandas==2.2.*",
  "asyncpg>=0.29",
  "pgvector~=0.5.0",
  "pydantic==2.11.*",
  "pygam~=0.12.0",
  "pymilvus[milvus-lite]~=3.0.1",
  "ragas>=0.3,<0.5",
  "langchain-community<0.4.2",
  "scikit-learn==1.8.*",
  "unitxt~=1.26.1",
]


[project.urls]
Homepage = "https://github.com/IBM/ai4rag"
Repository = "https://github.com/IBM/ai4rag"
Issues = "https://github.com/IBM/ai4rag/issues"
Documentation = "https://ibm.github.io/ai4rag/"


[project.optional-dependencies]
dev = [
  "ai4rag[test]",
  "ai4rag[code_check]",
  "ai4rag[docs]",
  "beautifulsoup4",     # reading data from html
  "dotenv",
  "ipykernel",
]

test = [
  "ai4rag[text-extraction]",
  "pytest",
  "pytest-cov",
  "pytest-mock",
  "psutil",
  "nbformat",
]

code_check = ["pylint", "black", "isort"]

text-extraction = [
  "docling-slim[standard,format-opendocument,format-audio]~=2.121.0",
]

docs = [
  "mkdocs~=1.6.0",
  "mkdocs-material~=9.5",
  "mkdocstrings[python]>=0.25.0",
  "mkdocs-git-revision-date-localized-plugin>=1.2.0",
  "mkdocs-minify-plugin>=0.8.0",
]

[tool.setuptools.dynamic]
version = { attr = "ai4rag.__version__" }

[tool.setuptools.package-data]
"ai4rag.assets_generator" = ["notebook_templates/*.ipynb"]

[tool.setuptools.packages.find]
exclude = ["tests*", "dev_utils*"]
namespaces = false


[tool.pytest.ini_options]
python_files = ["tests/**/*.py"]
log_cli = false
log_level = "INFO"
markers = [
  "milvus: tests that target the Milvus backend (server or embedded Milvus Lite).",
  "pgvector: tests that target the PostgreSQL + pgvector backend.",
]

[tool.coverage.run]
# Skipping orchestration modules that require integration tests (S3, multiprocessing, full experiment runs)
omit = [
  "ai4rag/core/experiment/experiment.py",
  "ai4rag/utils/data/text_extraction.py",
]

[tool.black]
line-length = 120

[tool.isort]
profile = "black"
py_version = 313
line_length = 120
skip = ["scripts/"]


[tool.pylint]
disable = [
  "missing-module-docstring",
  "too-few-public-methods",
  "arguments-differ",
  "import-outside-toplevel",
  "broad-exception-caught",
]
max-line-length = 120
max-args = 8
max-positional-arguments = 8
max-attributes = 10
max-module-lines = 1100
